[package]
name = "battle_registry"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
soroban-sdk = { version = "26.0.1", features = ["testutils"] }

[dev-dependencies]
soroban-sdk = { version = "26.0.1", features = ["testutils"] }
